# 🚀 Пошаговая инструкция: Деплой на Railway

Подробное руководство по развертыванию backend на Railway с нуля.

## Что такое Railway?

Railway - это платформа для деплоя приложений. Она автоматически:
- Запускает ваш код на их серверах
- Дает вам постоянный URL для доступа
- Безопасно хранит ваши секретные ключи

**Плюсы Railway:**
- 🆓 $5 бесплатных кредитов каждый месяц
- 🚀 Очень быстрый деплой
- 🔄 Автоматический редеплой при обновлении кода
- 📊 Удобный интерфейс

---

## ЧАСТЬ 1: Получение API ключа Claude

### Шаг 1.1: Регистрация на Anthropic

1. Откройте браузер и перейдите на https://console.anthropic.com/

2. Нажмите **"Sign Up"** (если у вас нет аккаунта)
   - Можно зарегистрироваться через Google
   - Или через email

3. Подтвердите email (если регистрировались через email)

### Шаг 1.2: Создание API ключа

1. После входа вы попадете в Console (панель управления)

2. В левом меню найдите и нажмите **"API Keys"**

3. Нажмите кнопку **"Create Key"** (Создать ключ)

4. Дайте ключу название, например: `flashcards-app`

5. Нажмите **"Create Key"**

6. **ВАЖНО!** Скопируйте ключ и сохраните в блокноте
   - Ключ выглядит примерно так: `sk-ant-api03-xxxxxxxxxxx...`
   - Он покажется только ОДИН раз!
   - Если потеряете - придется создавать новый

7. Держите ключ под рукой - он понадобится на шаге 2.6

---

## ЧАСТЬ 2: Деплой Backend на Railway

### Шаг 2.1: Регистрация на Railway

1. Откройте https://railway.app/

2. Нажмите **"Login"** или **"Start a New Project"**

3. Выберите способ входа:
   - **Рекомендуется: GitHub** (если у вас есть аккаунт на GitHub)
   - Или через email

4. Если выбрали GitHub:
   - Нажмите **"Login with GitHub"**
   - Разрешите Railway доступ к вашему GitHub аккаунту
   - Нажмите **"Authorize Railway"**

### Шаг 2.2: Создание нового проекта

1. После входа вы увидите Dashboard (главную страницу)

2. Нажмите кнопку **"New Project"** (большая фиолетовая кнопка)

3. Выберите **"Deploy from GitHub repo"**

   > Если это первый раз - Railway попросит разрешение доступа к GitHub

4. Нажмите **"Configure GitHub App"**

5. Откроется страница GitHub с настройками:
   - Выберите **"Only select repositories"** (Только выбранные репозитории)
   - Нажмите **"Select repositories"**
   - Найдите и выберите **"mypython"** (ваш репозиторий)
   - Нажмите **"Save"**

6. Вернитесь на вкладку Railway (она обновится автоматически)

### Шаг 2.3: Выбор репозитория и ветки

1. Теперь вы увидите список ваших репозиториев

2. Найдите и нажмите на **"DmitriyKalganov/mypython"**

3. Railway спросит какую ветку деплоить:
   - Выберите ветку **"claude/create-flashcard-app-011CURynhPwVzXQZqbHMxdvU"**

   > Это ветка, в которую мы добавили код

### Шаг 2.4: ⚠️ ВАЖНО - Указание папки backend

По умолчанию Railway попытается задеплоить весь репозиторий, но нам нужна только папка `flashcards-app/backend`.

**Вариант A - Через настройки сервиса (РЕКОМЕНДУЕТСЯ):**

1. После выбора репозитория Railway создаст сервис

2. Нажмите на карточку сервиса (она появится в центре экрана)

3. Перейдите на вкладку **"Settings"** (Настройки)

4. Найдите секцию **"Service"**

5. Найдите поле **"Root Directory"**

6. Введите: `flashcards-app/backend`

7. Нажмите за пределами поля (чтобы сохранить)

**Вариант B - Создать файл railway.json (альтернатива):**

Если вариант A не сработал, создадим конфигурационный файл:

```bash
# В папке flashcards-app/backend создайте файл railway.json
```

Содержимое файла:
```json
{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn app:app",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

### Шаг 2.5: Проверка деплоя

1. Railway автоматически начнет деплой

2. Вы увидите логи (текст) в реальном времени:
   ```
   Installing dependencies...
   Building application...
   Starting server...
   ```

3. Подождите 2-3 минуты

4. Если все хорошо, вы увидите:
   ```
   ✓ Build successful
   ✓ Deployment live
   ```

### Шаг 2.6: Добавление переменной окружения ANTHROPIC_API_KEY

**Это самый важный шаг!** Здесь мы добавляем ваш секретный API ключ.

1. Нажмите на вашу карточку сервиса (если не нажата)

2. Перейдите на вкладку **"Variables"** (Переменные)

3. Нажмите кнопку **"New Variable"**

4. В поле **"Variable Name"** введите:
   ```
   ANTHROPIC_API_KEY
   ```

   > Важно: название должно быть ТОЧНО таким! С большими буквами!

5. В поле **"Value"** вставьте ваш API ключ от Claude:
   ```
   sk-ant-api03-ваш-ключ-который-вы-скопировали-в-шаге-1
   ```

6. Нажмите **"Add"** (Добавить)

7. Railway автоматически перезапустит сервис с новой переменной

   > Это займет 1-2 минуты

### Шаг 2.7: Получение публичного URL

Теперь нужно получить адрес вашего backend сервера.

1. Нажмите на карточку вашего сервиса

2. Перейдите на вкладку **"Settings"** (Настройки)

3. Найдите секцию **"Networking"** (Сеть)

4. Найдите **"Public Networking"**

5. Нажмите кнопку **"Generate Domain"** (Сгенерировать домен)

6. Railway создаст URL вида:
   ```
   https://flashcards-backend-production-xxxx.up.railway.app
   ```

7. **СКОПИРУЙТЕ ЭТОТ URL!** Он понадобится для frontend

8. Сохраните его в блокнот

### Шаг 2.8: Проверка работоспособности

Давайте проверим, что backend работает!

1. Скопируйте ваш URL из шага 2.7

2. Добавьте к нему `/health`
   ```
   https://ваш-url.railway.app/health
   ```

3. Откройте этот URL в браузере

4. Вы должны увидеть:
   ```json
   {
     "status": "ok",
     "message": "Server is running"
   }
   ```

5. ✅ Если видите это - backend работает отлично!

6. ❌ Если видите ошибку:
   - Вернитесь к шагу 2.6 и проверьте API ключ
   - Проверьте логи на вкладке "Deployments"

---

## ЧАСТЬ 3: Настройка Frontend

Теперь нужно указать frontend, где находится backend.

### Шаг 3.1: Обновление конфигурации

1. Откройте вашу папку с проектом на компьютере

2. Перейдите в папку `flashcards-app`

3. Создайте файл `.env.production` (если его нет)

4. Откройте `.env.production` в текстовом редакторе

5. Добавьте строку:
   ```
   VITE_API_URL=https://ваш-url-из-шага-2-7.railway.app
   ```

   Например:
   ```
   VITE_API_URL=https://flashcards-backend-production-xxxx.up.railway.app
   ```

6. Сохраните файл

### Шаг 3.2: Сборка frontend

Откройте терминал (командную строку) и выполните:

```bash
# Перейдите в папку приложения
cd путь/к/mypython/flashcards-app

# Установите зависимости (если еще не установлены)
npm install

# Соберите приложение
npm run build
```

Вы увидите:
```
vite v5.0.8 building for production...
✓ built in 3.45s
```

---

## ЧАСТЬ 4: Создание Telegram бота

### Шаг 4.1: Создание бота через BotFather

1. Откройте Telegram на телефоне или компьютере

2. В поиске найдите: `@BotFather`

3. Откройте чат с BotFather

4. Отправьте команду: `/newbot`

5. BotFather спросит: "Alright, a new bot. How are we going to call it?"

   Введите название бота, например:
   ```
   Flashcards AI Learning
   ```

6. BotFather спросит username:

   Введите уникальное имя (должно заканчиваться на `bot`):
   ```
   flashcards_ai_learning_bot
   ```

   > Если имя занято - придумайте другое, например: flashcards_learn_bot

7. BotFather отправит сообщение с токеном:
   ```
   Done! Congratulations on your new bot...
   Use this token to access the HTTP API:
   1234567890:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw
   ```

8. Сохраните токен (он понадобится позже, если захотите добавить функции)

### Шаг 4.2: Настройка Web App кнопки

1. В том же чате с BotFather отправьте: `/mybots`

2. Нажмите на название вашего бота в списке

3. Выберите **"Bot Settings"**

4. Выберите **"Menu Button"**

5. Выберите **"Edit menu button URL"**

6. Отправьте URL вашего frontend:

   **Если используете GitHub Pages:**
   ```
   https://dmitriykalganov.github.io/mypython/
   ```

   **Если деплоили на Vercel/Netlify:**
   ```
   https://ваш-url.vercel.app
   ```

7. BotFather спросит: "Send me a text for the button"

   Введите:
   ```
   Открыть приложение
   ```

8. Готово! BotFather ответит: "Success!"

### Шаг 4.3: Тестирование

1. Найдите вашего бота в Telegram (по username из шага 4.1)

2. Откройте чат с ботом

3. Нажмите кнопку **"Menu"** (или **"Открыть приложение"**)

4. Приложение должно открыться внутри Telegram!

5. Попробуйте создать карточки - теперь это безопасно, API ключ на сервере!

---

## 🎉 Готово! Что дальше?

### Проверьте работу:

1. ✅ Backend работает (проверили в шаге 2.8)
2. ✅ Frontend собран (шаг 3.2)
3. ✅ Telegram бот создан (шаг 4.2)
4. ✅ Приложение открывается в Telegram (шаг 4.3)

### Дополнительные настройки бота (опционально):

**Добавить описание:**
```
/setdescription
Выберите бота
Введите: Изучайте языки с AI-генерированными флеш-карточками
```

**Добавить "About":**
```
/setabouttext
Выберите бота
Введите: AI-приложение для изучения иностранных языков через флеш-карточки
```

**Добавить аватар:**
```
/setuserpic
Выберите бота
Отправьте картинку (512x512 пикселей)
```

---

## ❓ Решение проблем

### Проблема 1: "API key not configured"

**Причина:** API ключ не добавлен или добавлен неправильно

**Решение:**
1. Вернитесь к шагу 2.6
2. Проверьте название переменной: должно быть `ANTHROPIC_API_KEY` (точно!)
3. Проверьте, что ключ скопирован полностью
4. После изменения подождите 1-2 минуты - Railway перезапустит сервис

### Проблема 2: Railway не видит папку backend

**Решение:**
1. Проверьте шаг 2.4
2. В Settings → Root Directory должно быть: `flashcards-app/backend`
3. Убедитесь, что путь написан без ошибок

### Проблема 3: /health возвращает ошибку

**Решение:**
1. Откройте Railway
2. Перейдите на вкладку "Deployments"
3. Нажмите на последний деплой
4. Посмотрите логи - там будет описание ошибки
5. Чаще всего: неправильный API ключ или не та папка

### Проблема 4: Приложение не открывается в Telegram

**Решение:**
1. Проверьте URL в шаге 4.2
2. URL должен быть с HTTPS (не HTTP!)
3. Попробуйте открыть URL в обычном браузере - работает?
4. Если в браузере работает, а в Telegram нет - подождите 5-10 минут

### Проблема 5: "Failed to fetch" в приложении

**Решение:**
1. Проверьте файл `.env.production` (шаг 3.1)
2. URL должен совпадать с URL из Railway (шаг 2.7)
3. Пересоберите frontend: `npm run build`

---

## 💰 Стоимость

### Railway:
- **Бесплатно**: $5 кредитов в месяц
- Этого хватит на ~100-500 запросов к AI в месяц
- Если закончатся - можно добавить карту (платить только за использование)

### Claude API:
- **Генерация 10 карточек**: ~$0.01 (1 цент)
- **1000 генераций**: ~$10
- Для личного использования: ~$1-5 в месяц

### Telegram бот:
- **Полностью бесплатно!**

---

## 📞 Нужна помощь?

Если что-то не получается:

1. **Проверьте раздел "Решение проблем"** выше
2. **Посмотрите логи** на Railway (вкладка Deployments)
3. **Создайте issue** в GitHub репозитории
4. **Напишите мне** - помогу разобраться!

---

## ✅ Чеклист (проверьте что сделали):

- [ ] Получил API ключ от Claude (шаг 1.2)
- [ ] Зарегистрировался на Railway (шаг 2.1)
- [ ] Подключил GitHub репозиторий (шаг 2.2-2.3)
- [ ] Указал папку backend (шаг 2.4)
- [ ] Добавил переменную ANTHROPIC_API_KEY (шаг 2.6)
- [ ] Получил публичный URL (шаг 2.7)
- [ ] Проверил /health endpoint (шаг 2.8)
- [ ] Создал .env.production (шаг 3.1)
- [ ] Собрал frontend (шаг 3.2)
- [ ] Создал Telegram бота (шаг 4.1)
- [ ] Настроил кнопку Web App (шаг 4.2)
- [ ] Протестировал в Telegram (шаг 4.3)

Если все галочки стоят - **поздравляю, вы молодец! 🎉**
