# Развёртывание Affiliate Bridge на Railway

## Быстрый деплой на Railway

### 1. Подготовка

Railway автоматически определит ваш Python проект и использует:
- `requirements.txt` для установки зависимостей
- `Procfile` или `railway.toml` для команды запуска
- `runtime.txt` для версии Python

### 2. Деплой через GitHub

1. **Отправьте код на GitHub:**
   ```bash
   git add .
   git commit -m "Prepare for Railway deployment"
   git push origin main
   ```

2. **Подключите Railway к GitHub:**
   - Зайдите на https://railway.app
   - Войдите через GitHub
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий `mypython`

3. **Настройте переменные окружения в Railway:**

   В разделе **Variables** добавьте:

   ```
   SECRET_KEY=генерируйте-случайную-строку-32-символа
   FLASK_ENV=production
   ```

   Railway автоматически установит:
   - `PORT` - порт приложения
   - `RAILWAY_ENVIRONMENT` - production

### 3. Деплой через Railway CLI

```bash
# Установите Railway CLI
npm i -g @railway/cli

# Войдите в Railway
railway login

# Инициализируйте проект
railway init

# Разверните приложение
railway up

# Откройте приложение
railway open
```

### 4. После деплоя

Railway автоматически:
- ✅ Установит зависимости из `requirements.txt`
- ✅ Запустит приложение через gunicorn
- ✅ Создаст базу данных SQLite
- ✅ Предоставит публичный URL

### 5. Рекомендации для production

#### Используйте PostgreSQL вместо SQLite

1. В Railway добавьте PostgreSQL плагин:
   - В проекте нажмите "+ New"
   - Выберите "Database" → "PostgreSQL"

2. Railway автоматически создаст переменную `DATABASE_URL`

3. Обновите `requirements.txt`:
   ```
   psycopg2-binary==2.9.9
   ```

4. База данных будет создана автоматически при первом запуске

#### Настройте домен

1. В Railway перейдите в **Settings** → **Domains**
2. Нажмите "Generate Domain"
3. Или подключите свой домен

#### Мониторинг

Railway предоставляет:
- Логи в реальном времени
- Метрики CPU/RAM
- Автоматические деплои при push в GitHub
- Откат к предыдущим версиям

### 6. Проверка работы

После деплоя проверьте:

```bash
# Замените URL на ваш Railway URL
curl https://your-app.railway.app/api/offers

# Должно вернуть JSON с офферами
```

### 7. Переменные окружения (полный список)

| Переменная | Описание | Обязательна |
|------------|----------|-------------|
| SECRET_KEY | Секретный ключ Flask | ✅ Да |
| DATABASE_URL | URL базы данных | ❌ (автоматически) |
| PORT | Порт приложения | ❌ (автоматически) |
| FLASK_ENV | Окружение | ❌ (production) |

### 8. Устранение неполадок

#### "No start command found"
✅ **Решение:** Создайте `Procfile` или `railway.toml` (уже создано!)

#### "Module not found"
✅ **Решение:** Проверьте `requirements.txt` и `wsgi.py`

#### "Database locked"
✅ **Решение:** Используйте PostgreSQL для production

#### Приложение падает
✅ **Решение:** Проверьте логи в Railway dashboard

### 9. Альтернативные платформы

Этот же проект можно развернуть на:

- **Render.com** (используйте те же файлы)
- **Heroku** (используйте те же файлы)
- **Google Cloud Run**
- **AWS Elastic Beanstalk**
- **DigitalOcean App Platform**

## Готово! 🚀

После деплоя ваша платформа будет доступна по адресу:
```
https://your-app.railway.app
```

---

## Быстрые команды

```bash
# Просмотр логов
railway logs

# Перезапуск
railway restart

# Открыть в браузере
railway open

# Статус деплоя
railway status
```
